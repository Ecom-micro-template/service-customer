# Service Customer - Environment Variables

# Server
APP_PORT=8004
APP_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=customer_db
DB_SSLMODE=disable

# JWT
JWT_SECRET=your-secret-key-change-this-in-production

# Auth Service (for future integration)
AUTH_SERVICE_URL=http://localhost:8001

# NATS (for future event publishing)
NATS_URL=nats://localhost:4222
